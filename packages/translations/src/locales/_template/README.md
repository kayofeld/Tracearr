# Locale template

This folder mirrors the current namespaces with English values, as the reference layout for a locale folder.

Do not copy it to add a language by hand. Locale folders are managed by Crowdin: English under `en/` is the source, and translated values sync back from there. To contribute a translation, use the Crowdin project instead of opening a pull request that edits locale JSON.

# Improvements backlog - <project>

Candidate improvements to the agents/process, drawn from the communication log. `/retrospective` works the
**open** items; the `process-optimizer` proposes fixes, the affected agent reviews, then they're applied.

Status: `open` | `proposed` | `applied` | `deferred`

| id  | issue (communication/process) | proposed improvement | target (agent / CLAUDE / command) | status |
| --- | ----------------------------- | -------------------- | --------------------------------- | ------ |
